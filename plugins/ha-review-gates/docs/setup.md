# Review Gates Setup

This plugin bundles conservative hook templates and review skills.

Codex hook support must be enabled by the user's local Codex configuration. The hook scripts are warning-oriented and do not assume a fail-closed API unless Codex clearly supports it.

