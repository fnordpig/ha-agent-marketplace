# Rollback

Before deployment, identify the backup or Vibecode/Git version to restore.

Every deployment report should include:

- files changed
- validation result
- backup identifier
- deploy status
- rollback command or UI path

Do not deploy if rollback is unknown.

