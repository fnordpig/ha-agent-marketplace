# Repo Layout

Prefer:

```text
automations/agent_*.yaml
packages/*.yaml
dashboards/*.yaml
docs/entity-map.md
docs/rollback.md
```

Avoid casual edits to `automations.yaml`, `.storage`, and `secrets.yaml`.

