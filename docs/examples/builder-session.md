# Builder Session

Prompt:

> Create a new motion lighting package for the hallway, but only propose a diff.

Expected behavior: prefer helpers and native triggers, write an agent-managed YAML proposal, and avoid direct live deployment.

